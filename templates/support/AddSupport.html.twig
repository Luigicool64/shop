{% extends 'base.html.twig' %}

{% block title %}{{parent()}}- ajouter un support{% endblock %}

{% block body %}
<h1 class="text-center text-primary mt-4 pt-4 display-1 fw-bold">ajouter un support</h1>
<div class="container-fluid">
    <div class="row justify-content-center">
        <div class="col-10 text-primary">
            {{form(form)}}
        </div>
    </div>
</div>

{% endblock %}